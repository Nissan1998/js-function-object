// multiplication of 12
function num(){
    for(var i=1; i<=10; i++){
        console.log(`12 x ${i} = ${12*i}`); 
    }
}
num()

// ২. একটা ফাংশন লিখবা যেটা যেকোন নামকে uppercase বা রেগুলার কেইস হিসেবে নিবে আর আউটপুট হিসেবে সেই নাম lowercase করে রিটার্ন করবে।
function toLowerCase(Mosaraf Hossain Nissan) {
    return name.toLowerCase();
}

toLowerCase();